function sayHello(name) {
  console.log("Hello " + name);
}

sayHello("Kiki");

// So here, basically you call this file on the command prompt like this:
// ...Nodejs -Mosh\first-app> node app.js
// And the output is:
// Hello Kiki

// Note that the command prompt should be in the first-app directory.
